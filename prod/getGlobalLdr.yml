
scenarios:
  - flow:
    - loop:
      - log: "-----------------------------------------Generated new virtual user-----------------------------------------"
      - get: #GET CHALLENGE CONTRIBUTORS
          url: "/api/Leaderboard/global?SortBy=globes&Ascending=false&PageNumber=1&PageSize=50"

          capture:
            - json: "$.status"
              as: status
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: data ##[0].status
      - log: "-----------------------------------------------Ended a scenario-----------------------------------------------"
      count: 1
